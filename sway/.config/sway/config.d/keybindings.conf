#################################
# KEYBINDINGS
#################################

bindsym $mod+Return        exec $term
bindsym $mod+Shift+q      kill
bindsym $mod+ctrl+space   exec $menu
bindsym $mod+ctrl+shift+space   exec $menu_cmd

bindsym $mod+Shift+r reload
bindsym $mod+Shift+e exec swaynag -t warning

bindsym $mod+e exec nemo

# Catppuccin Mocha
bindsym super+Shift+l exec swaylock -f --color 1e1e2e

floating_modifier $mod normal

# Volume wheel
bindsym XF86AudioRaiseVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Media
bindsym XF86AudioPlay exec playerctl play-pause

# Focus
bindsym $mod+$left  focus left
bindsym $mod+$down  focus down
bindsym $mod+$up    focus up
bindsym $mod+$right focus right

bindsym $mod+Left   focus left
bindsym $mod+Down   focus down
bindsym $mod+Up     focus up
bindsym $mod+Right  focus right

# Move
bindsym $mod+Shift+$left  move left
bindsym $mod+Shift+$down  move down
bindsym $mod+Shift+$up    move up
bindsym $mod+Shift+$right move right

bindsym $mod+Shift+Left   move left 
bindsym $mod+Shift+Down   move down
bindsym $mod+Shift+Up     move up 
bindsym $mod+Shift+Right  move right 

# Outputs
bindsym $mod+Ctrl+$up move container to output up; focus up
bindsym $mod+Ctrl+$down move container to output down; focus down

bindsym super+space input * xkb_switch_layout next

# Workspaces
set $ws /home/bgduke/.config/sway/config.d/scripts/workspace.sh

bindsym $mod+1 exec $ws 1
bindsym $mod+2 exec $ws 2
bindsym $mod+3 exec $ws 3
bindsym $mod+4 exec $ws 4
bindsym $mod+5 exec $ws 5
bindsym $mod+6 exec $ws 6
bindsym $mod+7 exec $ws 7
bindsym $mod+8 exec $ws 8
bindsym $mod+9 exec $ws 9
bindsym $mod+0 exec $ws 10

bindsym $mod+Shift+1 exec $ws 1 move
bindsym $mod+Shift+2 exec $ws 2 move
bindsym $mod+Shift+3 exec $ws 3 move
bindsym $mod+Shift+4 exec $ws 4 move
bindsym $mod+Shift+5 exec $ws 5 move
bindsym $mod+Shift+6 exec $ws 6 move
bindsym $mod+Shift+7 exec $ws 7 move
bindsym $mod+Shift+8 exec $ws 8 move
bindsym $mod+Shift+9 exec $ws 9 move
bindsym $mod+Shift+0 exec $ws 10 move

# Layouts and window state
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+v layout toggle split

bindsym $mod+f fullscreen
bindsym $mod+t floating toggle; border pixel 2
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# Scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+Shift+m scratchpad show

mode "resize" {
    bindsym $left  resize shrink width 80px
    bindsym $down  resize grow height 80px
    bindsym $up    resize shrink height 80px
    bindsym $right resize grow width 80px

    bindsym Left  resize shrink width 80px
    bindsym Down  resize grow height 80px
    bindsym Up    resize shrink height 80px
    bindsym Right resize grow width 80px

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

mode "special" {
  bindsym c exec sh -c 'wl-color-picker clipboard; notify-send "Color copied"'; mode "default"

  bindsym s exec slurp | grim -g - - | wl-copy \
    && notify-send "Screenshot copied"; mode "default"

  bindsym a exec grim -g "$(slurp -o -f '%x,%y %wx%h')" \
    "$SCREENSHOT_DIR/screenshot-$(date +%Y%m%d-%H%M%S).png" \
    && notify-send "Window screenshot saved"; mode "default"

  bindsym Shift+s exec grim -g "$(slurp)" \
    "$SCREENSHOT_DIR/screenshot-$(date +%Y%m%d-%H%M%S).png" \
    && notify-send "Screenshot saved"; mode "default"

  bindsym f exec grim \
    "$SCREENSHOT_DIR/screenshot-$(date +%Y%m%d-%H%M%S).png" \
    && notify-send "Fullscreen screenshot saved"; mode "default"

  bindsym Escape mode "default"
  bindsym Return mode "default"
}

bindsym $mod+z mode "special"
